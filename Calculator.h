#include <stdio.h>
#include <string.h>

void greeting();

void decision();

void decision_2();

static int add(int num1, int num2);

static int sub(int num1, int num2);

static int divi(int num1, int num2);

static int mul(int num1, int num2);

static int add_frac(int nom1_result, int dom1_result);



int num1, num2, addition, subtraction, divison, multiplication, numerator, denominator, numerator_2, denominator_2;
int nom1_result, dom1_result;
char answer[10], answer2[10], answer3[10];

